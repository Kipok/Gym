# Chain to existing resource server + agent config
config_paths:
  - resources_servers/math_with_judge/configs/math_with_judge.yaml
  - benchmarks/judge_luna.yaml

# Keep historical instance names for existing CLI commands and metric consumers.
# The implementation is math_with_judge: boxed answers, symbolic-first grading,
# and Luna fallback with a second swapped-order check after a positive verdict.
apex25_math_with_autograder_resources_server:
  _inherit_from: math_with_judge
  resources_servers:
    math_with_judge:
      should_use_judge: true
      judge_model_server:
        type: responses_api_models
        name: judge_model

apex25_math_with_autograder_simple_agent:
  _inherit_from: math_with_judge_simple_agent
  responses_api_agents:
    simple_agent:
      resources_server:
        name: apex25_math_with_autograder_resources_server
      datasets:
      - name: apex25
        type: benchmark
        jsonl_fpath: benchmarks/apex25/data/apex25_benchmark.jsonl
        prompt_config: benchmarks/apex25/prompts/default.yaml
        prepare_script: benchmarks/apex25/prepare.py
